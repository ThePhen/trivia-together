# version: '3.8'
services:
  backend-ts:
    image: tt-backend-ts:v0.0.0
    environment:
      NODE_ENV: production
    ports:
      - "3010:3000"
  frontend-client-react:
    image: tt-frontend-client-react:v0.0.0
    environment:
      NODE_ENV: production
    ports:
      - "3020:3000"
  frontend-admin-react:
    image: tt-frontend-admin-react:v0.0.0
    environment:
      NODE_ENV: production
    ports:
      - "3030:3000"
